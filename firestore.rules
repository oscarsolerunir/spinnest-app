rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite acceso público (solo para pruebas)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
